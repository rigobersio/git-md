var texto;
texto = `el comando para crear el primer commit es: 

git 		commit 		-m 		'mensaje del comit'
comando		opcion		argumento


git commit -m "first commit"
git branch -M main // esto creara una branch con el nombre de main 
es decir el repo local ya no será master será la branch main (main principal)
git remote add origin https://github.com/...
git push -u origin main // se puchiara o empujara a origin 
(origin  estará en github desde la rama main)

`


console.log(texto);
